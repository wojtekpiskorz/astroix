---
'@wojciechpiskorz/astroix': patch
---

`bun run smoke`: one command for the owner's manual-smoke environment — installs root + fixture, builds the package (node dist + chrome bundle), refreshes the fixture's `file:` dependency and boots the dev server on :4312 with pointers to `docs/manual-smoke.md`.
