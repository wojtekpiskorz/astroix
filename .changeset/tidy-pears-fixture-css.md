---
'@wojciechpiskorz/astroix': patch
---

E2E fixture CSS surface: a scoped `<style>` block on `.hero-title` (the `[data-astro-cid-*]` case), a second same-file rule and an `@media` rule in `home.css` (multi-range and badge cases for the rule list).
