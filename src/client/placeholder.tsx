/**
 * Placeholder chrome content — proves the whole delivery path (virtual
 * module → source mode → React mount → fast-refresh). The real shell
 * (shadow DOM app wrapping the canvas iframe) lands with its own slice.
 */
export function Placeholder() {
  return <strong>astroix</strong>;
}
