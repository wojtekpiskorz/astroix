# deps
node_modules/

# build
dist/
*.tsbuildinfo

# env & secrets
.env
.env.*

# astro dev artifacts (when dogfooding inside this repo)
.astro/

# tests
coverage/
crap-table.md
playwright-report/
test-results/

# os / editor
.DS_Store
*.log

# npm pack smoke lane artifact
*.tgz
# pack smoke fixture: the tarball regenerates per build — its lockfile cannot be pinned
e2e/pack-fixture/bun.lock
