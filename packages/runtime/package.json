{
  "name": "@wojciechpiskorz/astroix-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "The control plane — deep seams born incrementally per the rewrite DAG (ADR-0006 §9): the canonical project registry persistence first (#221); session supervision, project runtime, and edit authority land in their own lanes. The package barrel (index.ts) is owned by a later integration lane.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "exports": {
    "./registry": "./registry/project-registry.ts"
  },
  "sideEffects": false,
  "dependencies": {
    "@wojciechpiskorz/astroix-protocol": "^0.0.0",
    "zod": "^4.3.6"
  }
}
